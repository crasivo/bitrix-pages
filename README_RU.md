🔮 Bitrix Pages
===

Готовый модуль для 1C-Bitrix, который расширяет штатный функционала маршрутизации страниц.
Он отлично подойдет как для разработчиков, так и администраторов порталов (коробка).

Ключевые особенности (фичи):

- 💡 Универсальное (не отраслевое) решение для любой редакции.
- 💧 Современная архитектура, чистый код и простое масштабирование.
- 🧩 Не требуются сторонние модули и PHP расширения.
- 💫️ Новый роутер (MVC) без изменения структуры публичной части.
- 🔌 Изолированные REST контроллеры для подключения внешних систем.
- 🤹‍♂️ Большое количество типов страниц (см.ниже).

Возможные типы страниц:

- [x] `component` — произвольный Компонент;
- [ ] `custom` — зарезервированный тип для кастомизации;
- [ ] `file` — зарезервирован для скачивания файла _(b_file)_;
- [ ] `json` — зарезервирован для JSON контента;
- [x] `html` — произвольный HTML контент;
- [x] `include` — включаемая область _(bitrix:main.include)_;
- [ ] `php` — зарезервирован для PHP контента;
- [x] `redirect` — переадресация на любой другой URL.

> Все доступные типы объявлены в соответствующем списке-enum [PageContentType](lib/Domain/Api/PageContentType.php).

Возможные варианты применения:

1. Вложенные страницы в существующих разделах сайта (crm/tasks/etc).
1. Алиасы для старых страниц после изменения настроек компонентов (URL).
1. Кастомные страницы со своими статичным контентом, динамическими компонентами итд.
1. Ограничение доступа к файлам для сотрудников.

# 🚀 Установка

Минимальные требования для установки модуля:

- MySQL v8.0
- PHP v8.1
- Модуль `main` v25.700.0

### 1. Загрузить модуль

Расположение модуля: `/local/modules/crasivo.pages`

Пример команд для загрузки через консоль:

```shell
# Via composer
$ composer install crasivo/bitrix-pages
# Via git
$ git clone https://github.com/crasivo/bitrix-pages ./local/modules/crasivo.pages
# Via curl
$ mkdir -p ./local/modules/crasivo.pages
$ curl -sL https://github.com/crasivo/bitrix-pages/archive/refs/heads/main.tar.gz | tar -xzf - --strip-components 1 -C ./local/modules/crasivo.pages
```

### 2. Установить и настроить модуль

Процесс установки стандартный, каких-либо дополнительных действий не требуется.
Установщик сам проверит окружение (сервер/контейнер) и предупредит вас о конфликтах.

> Не забудьте проверить и переключить маршрутизацию с UrlRewrite на новый Routing.

Полный список изменений в файловой системе:

- `/bitrix/.settings.php` — добавится новый файл с маршрутами в секции `routing`.
- `/(bitrix|local)/components/crasivo.pages/*` — дополнительные компоненты для отображения в публичке
- `/(bitrix|local)/routes/pages.php` — файл с маршрутами

> На официальном сайте есть документация по использованию [Routing](https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=43&CHAPTER_ID=013764).

# 🕹️ Эксплуатация

## Разработка

### Domain

Предметная область описана в пространстве имен `Crasivo\Pages\Domain`.
Там описаны все публичные интерфейсы, списки итд.
Модификация поведения доступна через штатный DI.
Для этого достаточно объявить свой класс/конструктор в файле [.settings.php](https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=43&LESSON_ID=2795).

Пример переопределения существующего сервиса "Создание маршрута-компонента":

```php
return [
    'services' => [
        'value' => [
            Crasivo\Pages\Domain\Api\CreatePageComponent::class => [
                'constructor' => fn() => new Your\Custom\Service(),
            ],
        ],
    ],
];
```

### Network

Сетевой слой разделен на две части: `rest` и `web`.
Основная часть хранится в пространстве имен `Crasivo\Pages\Network\(Rest|Web)`,
другая реализована через компоненты `crasivo.pages:*`.

> [!IMPORTANT]
> Доступ ко всем контроллерам ограничен фильтрами типа `Authentication` & `ModuleRight`.
> Другими словами, пользователь должен быть авторизован и иметь минимальные права доступа к самому модулю (см.настройки).

### Infrastructure

Инфраструктурный слой, связанный с Bitrix, хранится в отдельном зарезервированном пространстве имен `Crasivo\Pages\Integration`.

> [!NOTE]
> Прочие НЕ вендорные интеграции предполагается хранить в пространстве `Infrastructure`.

> [!WARNING]
> Никогда не используйте и не обращайтесь к объектам из этого слоя без DI!
> Все классы помечены аттрибутом `@internal`, т.е для служебного пользования.
> Структура, вложенность и наименования могут измениться в любой момент (релиз).

### UseCase

> Данный слой и раздел в разработке.

# Сопровождение и поддержка

### 📌 TODO

Список плановых задач (хотелок):

1. Добавить дамп роутера в отдельный кэш-файл, чтобы лишний раз не обращаться в реестр.
1. Добавить другие виды запросов (POST/DELETE/PUT/etc).
1. Организовать полноценную связку User-Role-Permission на уровне групп, страниц, департаментов (Bitrix24).

---

## 📜 Лицензия

Данный проект распространяется по лицензии [MIT](https://en.wikipedia.org/wiki/MIT_License).
Полный текст лицензии можно прочитать в файле [LICENSE](LICENSE).
